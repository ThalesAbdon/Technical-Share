import React from "react";
import './Filters.scss';
import {Container} from 'react-bootstrap';


const Filters = () => (
    <Container className="d-flex justify-content-center">
        <section className="filters px-3 d-flex align-items-center">
            filtros?
        </section>
    </Container>
);

export default Filters;